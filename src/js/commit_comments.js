      // // Записываем почту перед отправкой
      // var emailText = document.querySelector(".form__input--email").value;
      // localStorage.setItem('emailText', emailText);

            // // Это срабатывает но на страницу вызова
            // $(".form__link--helper").click(function () {
            //   email.value = localStorage.getItem('emailText');
            // });

  // var text = document.querySelector(".form__input--email").value;

  // addEventListener('click', function(){
  // });

  // setTimeout(lel(),3000);

  // function lel() {
  //   console.log(localStorage.getItem( 'emailText'));
  // };

